<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <record id="group_employee_timesheet_portal" model="res.groups">
            <field name="name">Portal Employee Timesheet</field>
            <field name="implied_ids" eval="[(4, ref('base.group_portal'))]"/>
            <field name="comment">the user will have access to his own time sheet.</field>
            <field name="share" eval="True"/>
        </record>

        <record id="group_employee_timesheet_manager_portal" model="res.groups">
            <field name="name">Portal Employee Timesheet Manager</field>
            <field name="implied_ids" eval="[(4, ref('base.group_portal'))]"/>
            <field name="comment">the user will have access to all time sheets.</field>
            <field name="share" eval="True"/>
        </record>

    </data>
</odoo>

