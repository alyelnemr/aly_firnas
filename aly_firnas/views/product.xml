<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="aly_view_template_property_form" model="ir.ui.view">
            <field name="name">product.template.stock.property.form.inherit</field>
            <field name="model">product.template</field>
            <field name="priority">2</field>
            <field name="inherit_id" ref="product.product_template_only_form_view"/>
            <field name="arch" type="xml">
                <data>
                    <field name="categ_id" position="after">
                        <span name="update_cost_price" invisible="0">

                        </span>
                    </field>

                    <xpath expr="//header/button[last()]" position="after">
                        <button name="action_state_approve" attrs="{'invisible': [('state', '=', 'draft')]}"
                                string="Reset to draft" type="object"/>
                        <button name="action_state_approve" attrs="{'invisible': [('state', '=', 'approved')]}"
                                string="Approve" type="object" class="oe_highlight"/>
                        <field name="state" widget="statusbar" required="1"/>

                    </xpath>
                </data>
            </field>
        </record>
    </data>
</odoo>
